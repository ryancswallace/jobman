ZDOTDIR="$PWD/.asciinema-zsh" asciinema rec \
  --command "zsh -d" \
  --idle-time-limit 1.5 \
  --overwrite \
  casts/basic-demo.cast

DEMO_ROOT=$(mktemp -d)
jobman() { ./bin/jobman --state-dir "$DEMO_ROOT/state" "$@"; }
jobman clean --all --force
clear


---
jobman run --name slow --run-timeout 2s -- sleep 20

jobman wait slow

jobman show slow
---


agg \
  --select 1.3.. \
  --idle-time-limit 1.5 \
  --font-size 16 \
  casts/advanced_timeouts.cast \
  gifs/advanced_timeouts.gif