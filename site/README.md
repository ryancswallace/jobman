# Documentation site

GitHub Pages builds this directory with Jekyll and the Just the Docs theme.
Content intended for users belongs here; contributor and maintainer procedures
remain in the repository Markdown files linked from the site.

The production workflow is `.github/workflows/pages.yml`. To preview locally,
use any compatible Jekyll environment and serve `site/` as the source directory.
Do not commit `_site/` or other generated output.
